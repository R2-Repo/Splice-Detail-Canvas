---
description: Frozen splice routing — do not modify without explicit user approval
alwaysApply: true
---

# Frozen routing (user-approved only)

**Do not edit the symbols, files, or tests below unless the user explicitly asks in the current session.**

These implement 3161.4 BL bundle nesting, post-drag order, and loop-back bend fixes. Changes require user approval **and** must pass `npm run verify` (especially `visualQa3161.test.ts` + `spliceEdgeRouting.test.ts`).

## Frozen functions / hooks (`spliceEdgeRouting.ts`)

- `normalizeVisualCableIdForRouting`
- `recomputeRowOffsetsFromHandleYs`
- `assignSpliceRoutingLanesFromLiveHandles`
- `assignSpliceRoutingLanes` — global `jogX` after `assignSideVertLaneXs`
- `bundleJogXForMembers`
- `reconcileBundleJogXForRender`
- `sourceHorizWaypoints` (internal)
- `buildDemarcatedSplicePaths` — left-leg horizontal waypoints
- `hvDemarcatedSegments` — source horizontals
- `routingMidXForRender`
- `renderLaneGeometry` (internal)
- `publishDragRoutingSnapshot`
- `useRoutingLaneIndex` — drag snapshot + reconciled `jogX`

## Frozen canvas drag wiring (`WorkflowCanvas.tsx`)

- `refreshDragRouting`
- `onNodeDragStart` / `onNodeDrag` / `onNodeDragStop` — full-graph lane assign + `diagramCenterX`

## Frozen regression tests (do not weaken)

- `src/features/diagram/visualQa3161.test.ts`
- Loop-back / trunk tests in `src/features/canvas/edges/spliceEdgeRouting.test.ts`:
  - `reconcileBundleJogXForRender drops trunk when clamped midX would backtrack`
  - `buildDemarcatedSplicePaths skips reverse fan-out when jogX exceeds midX`
  - `anchors bundle trunk at the source side so fan-out has no loop-back`

## If a change is truly needed

1. Ask the user first; cite which frozen symbol and why.
2. Keep the diff minimal; do not refactor adjacent routing code.
3. Add or update a regression test before finishing.
4. Run `npm run verify`.
