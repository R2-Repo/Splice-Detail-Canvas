# Agent communication (Cursor)

Project-specific rules for the AI and **final response format** live in **`AGENTS.md`** at the repository root. Read and follow that file; this file is a pointer so the repo stays a single source of truth.

- Do not duplicate long rule lists here—update `AGENTS.md` if instructions change.
- `PROJECT_OVERVIEW.md` describes the product intent; keep it in sync with real project goals.
- First session on a fresh duplicate: follow **`docs/ONBOARDING.md`** so scope and workflow are defined before large feature work.
- **Memory and handoff:** read **`docs/MEMORY.md`** and **`docs/HANDOFF.md`** whenever more than one agent touches the work or the user resumes after a pause.
- **Docs index:** **`docs/README.md`** lists all governance docs (`DEFINITION_OF_DONE`, `QUALITY`, `SECURITY`, `MCP`, etc.).
