# The default static app does not require environment variables.
# If you add API keys or other secrets later, define them in a local
# .env file (and keep that file out of version control; see .gitignore).

# EXAMPLE_KEY=
